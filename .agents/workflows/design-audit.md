# /design-audit

Trigger the `design-polish-audit` skill on the current page or component
(or on a path the user specifies). Report the checklist results, then apply
any fixes that don't require further input from the user.
